const authController = require('./auth')
const apiController = require('./api')
const adminController = require('./admin')

module.exports = {
    authController,
    apiController,
    adminController,
}
